import React, {Fragment} from 'react';

const ComponentFunctional = () => {
    
    return (
        <Fragment>
            <div className="grid-item">
                <h2>My First Functional (Presentation)</h2>
                <p>This component is only for display HTML without logic (stateless).</p>
            </div>
        </Fragment>
    );
}

export default ComponentFunctional;